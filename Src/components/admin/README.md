# Admin

- Dashboard: `Src/components/admin/dashboard/admin-dashboard.html`

Notes:ocal API used by auth: `http://localhost:3000/users
- Requires the same l`.
- Only users with `role: "admin"` can access the dashboard.
